<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head'); %>

    <%- include('../partials/tinymc'); %>
</head>
<body>
<header id="header" style="background-color: #03045E">
    <%- include('../partials/header'); %>
</header>

<div class="container mb-4"
     style=" border: 5px solid #CAF0F8; border-radius: 10px; padding: 20px 30px 5px 30px; margin-top: 70px">
    <h1>Edit your startup project!</h1>
    <div class="col-12 py-auto">
        <form action="/project/edit/<%= project._id %>" method="POST">
            <div class="create_project_type">Title</div>
            <div class="col-12 create_project_content">
                <input class="create_project_input" name="title" type="text" value="<%= project.title %>"
                       placeholder="Artificial Intelligence to help with your insomnia" style="width: 100%"
                       required>
            </div>
            <div class="create_project_type">Type</div>
            <div class="select disabled" tabindex="1">
                <input class="selectopt" name="type" type="radio" id="opt1" value="<%= project.type %>" checked
                       disabled>
                <label for="opt1" class="option"><%= project.type %></label>
            </div>
            <div class="create_project_type">Shortly Description</div>
            <div class="col-12 create_project_content">
                <input class="create_project_input" name="shortly" type="text" value="<%= project.shortly %>"
                       placeholder="It's technology will correct your sleep mode!" style="width: 100%" required>
            </div>
            <div class="create_project_type">Date in days</div>
            <div class="select disabled" tabindex="2">
                <input class="selectopt" name="days" type="radio" id="opt6" value="<%= project.days %>" checked>
                <label for="opt6" class="option"><%= project.days %> days</label>
            </div>
            <div class="create_project_type">Goal in dollars</div>
            <div class="col-12 create_project_content">
                <input class="create_project_input" type="number" name="goal" min="1" max="1000000"
                       value="<%= project.goal %>"
                       placeholder="10 000$..."
                       style="width: 100%" required>
            </div>
            <textarea id="mytextarea" name="body"><%= project.body %></textarea>
            <div class="col-12 create_project_content justify-content-center">
                <a href="#" class="w-25">
                    <input class="submit my-4" type="submit"
                           style="width: 100%; font-size: 30px; height: 50px; float: left" value="Save Changes">
                </a>
            </div>
        </form>
    </div>
</div>
<%- include('../partials/footer') %>

<script>
    const fileUploader = document.getElementById('file-upload');

    fileUploader.addEventListener('change', (event) => {
        const feedback = document.getElementById('feedback');
        feedback.innerHTML = `✔`;
    });
</script>
</body>
</html>